<h9>Editting Book</h9>

<%= form_for(@book) do |f| %>

	<h9>Title</h9>
		<%= f.text_field :title %>


	<h9>Opinion</h9>
		<%= f.text_area :body %>

	<%= f.submit 'Update Book' %>

<% end %>

<%= link_to 'Show', book_path(@book.id) %>
<%= link_to 'Back', books_path %>

